# 界面美感对比 —— 智能水下清洁机器人控制系统（上位机）

> 目标：把本项目《智能水下清洁机器人控制系统》上位机界面（PySide2 + 定制 QSS）与业界主流开源水下/无人机地面站、以及通用 Qt 深色主题库进行对比，找出：**配色是否协调、排版是否清晰、控件质感是否到位、有哪些可借鉴点**。
>
> 说明：本篇基于各项目公开的**功能特性、界面语言、设计文档与配色惯例**进行分析，不直接截图比较。

---

## 0. 本项目界面现状（基准）

| 维度 | 现状 |
|---|---|
| 技术栈 | PySide2 + 自绘 QWidget（无 QML），pyqtgraph 用于声纳/预留曲线 |
| 主色 | 深蓝底层 `#0B132B` → 面板 `#14213D` |
| 强调色 | 青 `#31C4F3`、绿 `#3DDC97`、黄 `#FFD166`、红 `#FF5A5F` |
| 文字 | 主白 `#FFFFFF`，副灰蓝 `#8FA9C9`，数字用等宽字体 |
| 布局 | 左侧导航 + 顶部校徽/校名 + QStackedWidget 多页（监控/规划/控制/设置/数据/日志/声纳） |
| 控件质感 | 卡片面板 + 发光(glow) 状态灯、自绘深度表盘 DepthGauge、SVG 图标 |
| 展示模式 | 展会默认归零，勾选"动态演示"仅侧栏系统状态微动 |

**现状优点**：深蓝工业风明确、强调色统一、卡片层级清晰、有图标与等宽数字，已具备"专业监控"观感。
**现状可提升**：① 强调色使用较多（青/绿/黄/红并存），需明确"语义优先级"，避免画面"花"；② 面板以纯色卡片为主，缺少"玻璃质感/渐变/阴影"层次；③ 数字排版依赖 QLabel，缺乏 QGC 那种"大数值 + 小单位"层级；④ 声纳图表较空（按展会要求留空是合理的，但正常模式可增强）。

---

## 1. QGroundControl（QGC）—— C++/Qt 地面站标杆

- 仓库：https://github.com/mavlink/qgroundcontrol
- 定位：多平台地面站，支持无人机/无人车/水下(ArduSub)，**是水下 ROV 地面站的事实标准之一**。
- 界面语言：QSS + 大量自绘/TableView；Widget 化面板，卡片、状态条、数值标签、滑杆、诊断/参数工具页齐全。

**配色与观感**
- 采用**单色深色底 + 高饱和强调色**：整体偏"蓝/深灰"工业化，关键状态(armed / 飞行 / 电量)用红/绿/黄表达，强调色数量被严格约束，避免视觉噪声。
- 强调"**语义色**"：绿=正常、黄=警告、红=报警/危险，且同类状态全局一致（这是最值得抄的）。

**排版与控件质感**
- 大字号遥测数值（如高度/航向/速度）配小号单位与说明文字，形成**信息层级**；卡片化面板、留白适度。
- 顶/侧工具栏 + 折叠面板（Setup / Fly / Plan / Analyze）结构清晰，参数页使用**树形分组 + 搜索**。
- 视频与地图 Widget 化，支持拖拽布局。（我们不需要拖拽，但"信息分区 + 折叠"可借鉴）

**可借鉴点**
1. 强调色"语义化+收紧"：把青色固定为"水/声纳/数据"，绿色=正常，黄色=警示，红色=紧急，只在对应语义处使用。
2. 遥测数值采用"大数字 + 小单位 + 更浅描述文字"的三级排版。
3. 参数/日志/数据管理页做成**可搜索的分组表格**，而不是纯静态展示。

---

## 2. BlueOS + Cockpit（Blue Robotics）—— 现代 Web 控制台

- 仓库：https://github.com/bluerobotics/cockpit （AGPLv3）
- 文档：https://blueos.cloud/cockpit/docs/stable/usage/overview/
- 定位：BlueROV/BlueBoat 的**下一代地面站**，Widget 化、可在浏览器/移动端/VR 运行，BlueOS 生态（ArduSub 自驾）。

**配色与观感**
- 现代 Web/仪表盘风格，**深色背景 + 亮色单色强调**，科技感强；大量使用 **Widget（小卡片）自由排布**，每个 Widget 显示一组数据或一个视频。
- 强调"**可自定制视图(Views)**"：不同屏幕/设备可保存不同布局（JSON），这对演示与多屏监控非常友好。

**排版与控件质感**
- 组件化、扁平 + 轻阴影，使用清晰的图标字体/图标库，状态用**彩色指示条**呈现。
- 视频 Widget 支持多路 & 录像并带遥测字幕（telemetry subtitles），通知系统支持语音播报。
- 数据湖(Data Lake)统一管理变量，界面可实时订阅自定义 MAVLink 值。

**可借鉴点**
1. **视图/布局可切换**：我们已用多页(QStackedWidget)，可再加"常用/演示/全功能"模式切换，对应展会场景。
2. **Widget 语义标签与状态色**：把"正常/告警/离线"用统一小状态灯表达（我们已实现 StatusLight，可统一管理）。
3. 卡片化 + 统一工具栏 + 视频与"遥测字幕"叠加：把时间戳/状态写进视频角落，提升专业感。

---

## 3. OpenROV Trident —— 开源消费级水下机器人

- 仓库：https://github.com/OpenROV （Trident / trident-releases）
- 定位：面向大众的水下无人机，控制器基于树莓派 + Web 界面/配套 App，直接对应"树莓派91节点"这一场景。

**配色与观感**
- 偏向**明亮、友好、直观**的水下蓝/白风格（"人人可玩"定位），与 QGC 的专业暗色不同：更适合教育/会展，但少一点"工业监控"气质。

**可借鉴点**
1. 简单直观——把**最常用的两个操作优先级最高**（前进/俯仰/灯光/急停），其余收进次级菜单；这对展会"一眼看懂"有帮助。
2. 树莓派派节点这一"上位机 ↔ 91 节点"的轻量通信思想，与本项目一致。

---

## 4. QDarkStyleSheet —— 通用 Qt 深色主题

- 仓库：https://github.com/ColinDuquesnoy/QDarkStyleSheet （BSD-3）
- 定位：PyQt/PySide 通用暗色 QSS 主题，覆盖全部标准控件（按钮/表格/滑块/树/进度条/选项卡）。

**配色与观感**
- 深灰/蓝黑背景 + 青/蓝强调，整体**非常统一、沉稳**，是"最小可用且好看"的暗色主题代表。
- 突出**标准控件质感**（hover / pressed / disabled / focus 状态齐全），保证每一个控件都有清晰反馈。

**可借鉴点**
1. 补全**控件状态样式**：hover / pressed / disabled / focus，让按钮、滑杆、表格在交互时有反馈（当前实现偏向静态）。
2. 用一套**统一的边框/圆角/间距 token** 约束所有面板，肉眼观感高度一致。
3. 进度条/滑块采用"轨道 + 填充 + 圆角"处理（已在电池进度条实现，可推广到滑杆）。

---

## 5. qt-material —— Material 风格 Qt 主题

- 仓库：https://github.com/UN-GCPDS/qt-material / https://dunderlab.com
- 文档：https://qt-material.readthedocs.io/en/latest/
- 定位：Material Design 风格，**浅色/深色双主题、动态换主题色**，强调圆角、状态切换动画、卡片与浮层阴影。

**配色与观感**
- 大量使用**主色 + 辅色**theme 工具，一键换主题；卡片带阴影与圆角，**按钮有涟漪/按压动画**，观感现代。
- 支持 `theme='dark' / 'light'`，可直接切换，适合"展会演示一个、日常深色一个"。

**可借鉴点**
1. **主题可切换 + 动态主色**：把强调色做成可配置项（如青/蓝），演示时换色即可差异化。
2. **卡片阴影 + 圆角 + 适度动画**：给面板增加轻阴影与圆角，按钮加按压反馈，提升"质感"。
3. 用一等"卡片"组件统一来组织信息，减少裸标签成堆出现的杂乱感。

---

## 6. 汇总对比表

| 参考项目 | 技术栈 | 主色风格 | 强调色处理 | 排版/质感亮点 | 对本项目最值得借鉴 |
|---|---|---|---|---|---|
| QGroundControl | C++/Qt | 深蓝工业 | **语义色高度统一** | 大数+小单位层级、分组参数页、Widget 化面板 | ①收紧强调色语义 ②数值三级排版 ③参数/数据页可搜索分组表 |
| BlueOS Cockpit | Web/Qt | 现代深色仪表盘 | 单色高亮 | Widget 自由排布、视图可保存、视频+遥测字幕 | ①多模式视图（展会/全功能）②统一状态灯 ③视频叠加遥测 |
| OpenROV Trident | 树莓派+Web | 明亮友好 | 简洁直观 | 常用操作置顶、次级菜单收纳 | ①两级操作优先级 ②轻量"上位机↔91节点" |
| QDarkStyleSheet | PySide/PyQt | 深灰蓝黑 | 统一青/蓝 | **全控件交互状态齐全**、统一token | ①hover/pressed/focus 全状态 ②统一边框/圆角/间距 token |
| qt-material | PySide/PyQt | Material 深浅双主题 | 主题色可配置 | 卡片阴影圆角、按压动画、动态换色 | ①主题可切换 ②卡片阴影+圆角 ③强调色可配置 |

---

## 7. 结论与建议落地（按性价比排序）

1. **收紧密集用色（抄 QGC）**：把青/绿/黄/红改成"语义优先级"——青=水/声纳/数据，绿=正常，黄=预警，红=紧急；同类状态全局一致，避免画面"花"。
2. **遥测数值三级排版（抄 QGC）**：大号数值 + 小号单位 + 更浅说明文字；用等宽字体保证数字稳定（已部分实现，可推广到所有状态卡片）。
3. **补全控件交互状态（抄 QDarkStyleSheet）**：给按钮/滑杆/表格加 hover / pressed / disabled / focus 样式，让演示时交互有明确反馈。
4. **卡片质感升级（抄 qt-material）**：面板增加圆角 + 轻阴影 + 统一间距，强调色设为可配置项。
5. **多模式视图（抄 Cockpit）**：在现有 QStackedWidget 基础上增加"展会只读/全功能"切换，突出演示与日常两种诉求。
6. **状态灯与状态条统一（已有 StatusLight）**：统一管理所有"正常/告警/离线"指示，形成一致的语言。

> 综上：本项目已经在"深蓝工业风 + 语义强调色 + 卡片式布局 + 图标/等宽数字"上具备了不错的底子；进一步拉开差距的关键是**收紧用色语义、建立三级数值层级、补全控件交互状态、提升卡片质感**，这四点几乎都来自 QGC 与 QDarkStyleSheet 的成熟做法。
